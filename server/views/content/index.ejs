<% include ../partials/header.ejs %>

  <!-- MAIN CONTENT -->
  <div class="container">
    <div class="row">
      <h1>
        <%= title %>
      </h1>

      <div class="col-md-offset-3 col-md-6">
        <% if(displayName) { %>
          <h2>Welcome
            <%= title %>,
              <%= displayName %>
          </h2>
          <% } else { %>
            <h2>Welcome to
              <%= title %>
            </h2>
            <% } %>
      </div>
    </div>
  </div>

  <% include ../partials/footer.ejs %>